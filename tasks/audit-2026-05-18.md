# UX/a11y Code Review — 2026-05-18

**Branch (audit conducted on):** `fix/code-review-2026-05-11` (HEAD `b4af066`)
**Reviewer:** Jennifer Mitchelle (Senior UX Design Critic)

## Summary

Fresh-eye independent walk of all four Jinja2 templates (`base.html`, `index.html`, `generate.html`, `gallery.html`), the single stylesheet (`static/css/style.css`, 506 lines), the framework JS (`static/js/main.js`, 27 lines), and the Flask routes that templates depend on (`app.py`, 217 lines). **0 Critical, 0 High, 0 new Medium, 0 new Low.** All prior carry-overs (N16, M7, M8, N5–N10, N12–N17, L7–L17) remain logged as deferred. **Tenth consecutive log-only day** — the code working tree remains byte-identical to the 2026-05-09 post-fix commit (`d50e4b4`); nine audit-only commits since (`cc178bf`, `ef4ebf4`, `645a0bf`, `59ba420`, `949662c`, `2cc7479`, `2bc6be0`, `b4af066`, today's).

## Critical

**None.** All four prior C-series items (path-traversal regex, lazy cleanup, focus indicator, prefers-reduced-motion) remain correctly implemented at `app.py:21,27-38,73,85,95`; `style.css:40-48,457-472`.

## High

**None.** All nine prior H-series items (skip-link, `<main>` landmark, `lang`, viewport, descriptive alt text, `aria-live` regions, explicit `type="button"`) remain correctly implemented at `templates/base.html:12,34`; `templates/generate.html:33,88,109,26,80,97,101,113`; `templates/gallery.html:25`.

## Medium

**No new findings this pass.** Carried-over (unchanged from 2026-05-17):

| ID | Item | File:line |
|---|---|---|
| N16 | Generated band page lacks `<main>` and `<footer>` elements | `createAct.py:543-624` |
| M7 | Dead `marquee {}` rule in generated CSS | `createAct.py:530-536` |
| M8 | `bandPreview.innerHTML` injects unsanitised AI band name | `templates/generate.html:233-236` |
| N5 | Progress-steps table column header is the literal "?" | `templates/generate.html:46` |
| N6 | Step-indicator state changes not announced to SR users | `templates/generate.html:183-209` |
| N7 | Polling interval re-announces same prose message | `templates/generate.html:158, 183-191` |
| N8 | `cancelGeneration()` does not tell the server to stop | `templates/generate.html:249-254`, `app.py` |

## Low

**No new findings this pass.** Carried-over (unchanged from 2026-05-17):

| ID | Item | File:line |
|---|---|---|
| N9 | `#progressDisplay` `aria-live` region wraps too much markup | `templates/generate.html:33-85` |
| N10 | Failing step is invisible to user on error | `templates/generate.html:49-77` |
| N12 | `body` font-family includes `cursive` between specific and generic | `style.css:67`, `createAct.py:387` |
| N13 | `<hr>` `noshade`/`size` attributes obsolete in HTML5 | multiple templates, `createAct.py` |
| N14 | Heading hierarchy h2 → h4 skip in feature boxes | `templates/index.html:85,91,97` |
| N17 | No `<noscript>` fallback on `/generate` | `templates/generate.html:14-30` |
| L7 | `Faker==28.1.0` declared but unused | `requirements.txt` |
| L8 | Inline `import re` inside functions | `createAct.py:85, 218` |
| L9 | Visitor counter randomises on each page load (thematic) | `templates/base.html:47` |
| L10 | Dead `mailto:webmaster@aibandgen.geocities.com` (thematic) | `templates/base.html:52` |
| L11 | Visitor counter and `mailto` repeated in generated band pages (thematic) | `createAct.py:600, 616` |
| L12 | `<table>` used for layout in `index.html` "What You Get" section | `templates/index.html:81-102` |
| L13 | Inline styles throughout templates and generated HTML | many |
| L14 | Deprecated HTML attributes (`bordercolor`, `noshade`, `cellpadding`, `align`, `width`, `valign`) throughout | many |
| L15 | `band_assets()` route doesn't whitelist filenames | `app.py:82-87` |
| L16 | `current_year` computed once at module-load; stale across year boundary | `createAct.py:9, 618` |
| L17 | `.step-indicator` placeholder `...` at `#666666` on `#0a0a1a` ~3.36:1 (defensibly exempt under WCAG 1.4.3 inactive-component clause) | `static/css/style.css:367-371`; `templates/generate.html:50,54,58,62,66,70,74` |

## Independent fresh-eye spot-checks performed this pass

1. **Re-derived colour contrast for non-trivial surfaces** (independent computation, not relying on prior-audit tables):
   - `.footer-email a` `#ff69b4` on body bg `#000022` → 7.84:1 (AAA).
   - `.footer-copy` `#9999bb` on body bg `#000022` → 7.43:1 (AAA).
   - `.banner-tagline` `#ff69b4` on gradient stop `#000099` (worst case) → 5.51:1 (AA pass).
   - `.badge` `#888888` on `#111111` (≈0.722em effective) → 5.37:1 (AA pass for small text).
   - `.feature-box p` `#aaaaaa` on `#0a0a2a` → 8.41:1 (AAA).
   - `.intro-text` `#dddddd` on `#0a0a1a` → 14.3:1 (AAA).
   - `<strong style="color:#00ffff">` (`index.html:16`) on `#0a0a1a` → ~24:1 (AAA).
   - `.nav-bar a` `#00ff00` on `#111122` → >13:1 (AAA).
   - **No new contrast failures.**

2. **Heading hierarchy re-walk:**
   - `base.html`: site title is `<h1>` (line 19) ✓
   - `index.html`: h1 (base) → h2 (lines 7, 26, 78) → h4 (lines 85, 91, 97). **h2→h4 skip is N14 LOW carry-over.** ✓
   - `generate.html`: h1 (base) → h2 (line 6) → h3 (lines 16, 35, 90, 111). No skip. ✓
   - `gallery.html`: h1 (base) → h2 (line 6) → h3 (line 45, empty-state only). No skip. ✓

3. **Form/input accessibility:** Independently grep'd all four templates for `<input`, `<textarea>`, `<select>`, `<label>`. **Zero matches.** No form fields exist anywhere in the framework UI. All five `<button>` elements have explicit `type="button"` (`generate.html:26,80,97,101,113`) and visible text content — no `aria-label` needed. ✓

4. **Touch-target size (WCAG 2.5.8 AA, 24×24 min):**
   - `.nav-bar a` (`style.css:141-147`): 6+8+6 = 20px+text ≈ 32px height × variable width ≥ 30px. ✓ (24×24 met)
   - `.retro-button` (`style.css:277-287`): 10+10+1.1em ≈ 40px height. ✓
   - `.retro-button-small` (`style.css:306-319`): `min-height: 32px`. ✓
   - `.gallery-thumb` wrapped link area: 90×90 desktop / 60×60 mobile. ✓ (mobile target still ≥24×24)
   - `.skip-link` on focus: padding 8+8+text ≈ 32px. ✓
   - `.big-link`: inline text only — but it's a primary call-to-action link; line-height 1.5 × 1.2em font ≈ 25px effective height. **Marginal but PASSES 2.5.8 AA** (24×24 minimum); 2.5.5 AAA (44×44 enhanced) is not met but not required.

5. **`<table>`s without `<thead>`/`<tbody>`:** All four data tables (`info-table`, `gallery-table`, `progress-steps-table`, plus the layout table in `index.html:81`) use bare `<tr>` children of `<table>`. Browsers auto-insert `<tbody>`; this is valid HTML5 (parser inserts implicit `<tbody>` per HTML5 §13.2.6.4.9). `<th scope="col">` is correctly applied in all data tables (verified `index.html:30-32`, `gallery.html:16-18`, `generate.html:46-47`). No finding.

6. **Keyboard trap audit:** Walked `Tab` order conceptually through `generate.html` in each state:
   - Initial: skip-link → home → generate → gallery → generate-band button → footer email link. No trap.
   - Progress state: skip-link → nav → cancel button → footer email link. No trap.
   - Success state: skip-link → nav → view-band button → generate-another button → footer email link. No trap.
   - Error state: skip-link → nav → retry button → footer email link. No trap.
   - `focusHeading()` (`generate.html:211-217`) moves focus to `<h3 tabindex="-1">` on each state change; subsequent Tab moves into the visible button cluster. ✓

7. **ARIA misuse audit:** Independently re-checked every ARIA attribute in all four templates.
   - `aria-hidden="true"` on decorative banner stars (`base.html:18, 23`) — correct.
   - `aria-label="Primary"` on `<nav>` (`base.html:27`) — correct (disambiguates the single landmark nav).
   - `aria-current="page"` conditional on `request.endpoint` (`base.html:28-30`) — correct.
   - `aria-live="polite" aria-atomic="false"` on `#progressDisplay` (`generate.html:33`) — correct for incremental updates.
   - `aria-live="polite" aria-atomic="true"` on `#successDisplay` (`generate.html:88`) — correct for one-shot announce.
   - `role="alert" aria-live="assertive"` on `#errorDisplay` (`generate.html:109`) — redundant (alert implies assertive+atomic) but harmless.
   - `aria-atomic="false/true"` asymmetry is defensible per spot-check 1 of the 2026-05-17 audit.
   - `tabindex="-1"` on `<main>` and three `<h3>` headings — programmatically focusable only; correct pattern.
   - `scope="col"` on `<th>` in all three data tables — correct.
   - **No ARIA misuse found.**

8. **Independent grep sweep:** `onclick=`, `onsubmit=`, `javascript:`, `accesskey`, `autofocus`, positive `tabindex` — **zero matches** across all templates and CSS. ✓

9. **Responsive layout sanity-check** at 320px, 375px, 768px, 1024px viewports:
   - `@media (max-width: 640px)` collapses all three data tables to 100% width and reduces gallery thumbnails to 60×60.
   - `.page-wrapper` `max-width: 800px` with `margin: 0 auto` — no horizontal overflow.
   - `.feature-cell` becomes `display: block` on mobile — three-column "What You Get" stacks correctly.
   - No fixed-width values that would cause horizontal scroll at 320px.
   - **No responsive findings.**

10. **`prefers-reduced-motion` honouring:** Three layers (framework CSS `style.css:457-472` blanket-disables all animation/transition; framework JS `main.js:6-10` early-returns before starting the star-twinkle interval; generated-page CSS also honours per prior N15 fix). All three layers verified. ✓

## Phase 2 — Fixes

**None applied.** No Critical or High findings were identified. Per the user's instructions: "If you find no Critical or High issues, say so explicitly in `tasks/review.md` and skip to Phase 3 — do not invent issues to fix."

The seven Medium and seventeen Low carry-overs remain logged but unfixed, consistent with the documented disposition decisions in the 2026-05-09 through 2026-05-17 audit cycle.

## Phase 3 — Verification

- `python3 -c "import app"` — module imports cleanly with no syntax or import error.
- `python3 -m py_compile app.py createAct.py` — both compile cleanly.
- `python3` `html.parser.HTMLParser` over all four templates (`base.html`, `index.html`, `gallery.html`, `generate.html`) — zero parse errors.
- `git diff d50e4b4..HEAD -- '*.py' '*.html' '*.css' '*.js'` — empty (no code drift since the 2026-05-09 N18 post-fix commit `d50e4b4`; nine audit-only commits since).
- `git status --short` — only untracked `media/` (per instruction, not staged; verified orphan since 2026-05-16).
- Did not start the dev server (would burn API credits on `/api/generate`). Static template + source review covers all rendering paths the audit needs.

## Phase 4 — Deliver

- **No code changed.** Per the user's instructions: "If NO code was changed (only `tasks/review.md` updated), still commit and push `tasks/review.md` to the current branch (`fix/code-review-2026-05-11`) following the daily-audit pattern visible in `git log` — but do NOT open a new PR for an audit-only update."
- Today's commit will include this `tasks/review.md` plus a mirror copy at `tasks/audit-2026-05-18.md`, following the cadence of `2bc6be0`, `2cc7479`, `949662c`, `59ba420`, `645a0bf`, `ef4ebf4`, `cc178bf`. Untracked `media/` directory not staged.

## Summary table

| Severity | Count this audit | Fixed this pass |
|---|---|---|
| CRITICAL | 0 | — |
| HIGH | 0 | — |
| MEDIUM | 0 new (7 carried) | 0 (deferred) |
| LOW | 0 new (17 carried) | 0 (deferred) |

**No critical/high findings. No code changes this pass. Audit-only commit on `fix/code-review-2026-05-11`.**
