# UX/a11y Audit — 2026-05-11

**Branch (audit conducted on):** `fix/code-review-2026-05-10` (HEAD `cc178bf`)
**Fix branch:** `fix/code-review-2026-05-11` (branched from `fix/code-review-2026-05-10` since prior daily branches are stacked and not yet merged into `main`)
**Reviewer:** Jennifer Mitchelle (Senior UX Design Critic)

## Outcome

**0 Critical, 0 High. No new findings of any severity.** All prior carry-overs (M7–M8, N5–N10, N12–N17, L7–L15) remain logged as deferred in `tasks/review.md`. No fixes applied this pass; today's commit is the audit log only, matching the established "no critical/high findings" cadence — now the third consecutive log-only day (2026-05-08, 2026-05-10, 2026-05-11).

`git diff cc178bf..HEAD -- '*.py' '*.html' '*.css' '*.js'` returns no diff — the working tree for code is byte-identical to yesterday's post-N18-fix state. Untracked `media/` directory ignored per instruction.

## Methodology

Files re-reviewed in current tree:

- `templates/base.html` (62 lines)
- `templates/index.html` (105 lines, post-N18 fix)
- `templates/gallery.html` (59 lines)
- `templates/generate.html` (286 lines)
- `static/css/style.css` (507 lines)
- `static/js/main.js` (27 lines)
- `createAct.py` HTML emission and discography/member generation (lines 324–627)
- `app.py` routes and validation (217 lines)

Cross-checked the four pre-existing generated band-page outputs (`EchoesOfTheMirage/`, `MoonlitReverie/`, `TheVelvetEchoes/`, `**VelvetEchoes/`) — these were generated by an older `createAct.py` (pre-retro overhaul `8b971ef`) and use a different design entirely. They render via `view_band` but are historical artifacts. They contain pre-existing structural quirks (track-listing markup with stray "Album Title" text from the AI response, fixed-position footer that overlays content) which are NOT regressions caused by current code; they are baked into the static HTML on disk. Not in scope for fixing this pass — would require regenerating each band, which costs API credits and could lose the dated artifacts. **No code-side regression**: the current `create_html_content()` produces the new retro design which has been audited every day since the overhaul.

## Re-verification of all prior fixes (current tree)

| ID | Item | File:line | Status |
|---|---|---|---|
| C1–C4 (2026-05-01) | Path-traversal regex, lazy cleanup, focus indicator, prefers-reduced-motion | `app.py:21,27-38,73,85,95`; `style.css:40-48,457-472` | PASS |
| H1–H9 (2026-05-01) | Skip link, `<main>`, lang, viewport, alt, aria-live, button types, etc. | `templates/base.html:12,34`; `templates/generate.html:33,88,109` | PASS |
| N1 | Auxiliary-link wrapper colour ≥7:1 | `createAct.py:601` (`color:#999999`) | PASS |
| N2 | Top-nav `.nav-bar a` ≥24px tap target | `style.css:141-147` | PASS |
| N3 | `<th scope="col">` on data tables | `templates/gallery.html:16-18`, `templates/generate.html:46-47`, `templates/index.html:30-32` (post-N18) | PASS |
| N4 | `focusHeading()` moves focus on state changes | `templates/generate.html:211-217,224,237,246` | PASS |
| N15 | Generated-band-page `.nav-bar a` ≥24px tap target | `createAct.py:525-529` | PASS |
| N18 | `index.html` info-table `<th scope="col">` | `templates/index.html:30-32` | PASS |

## Checks performed

1. **Color contrast (WCAG 1.4.3 AA)** — re-derived worst cases against current values:
   - `.table-header-row` `#ff00ff` on `#1a1a3a` ≈ 4.85:1 (`style.css:240-249`)
   - `.banner-tagline` `#ff69b4` on banner-gradient peak `#000099` ≈ 5.47:1
   - Generated-page album-title `c1` on `#222222` worst is `#ff69b4` ≈ 6.5:1
   - Generated-page track-list `c3` on `#111111` worst is `#ff4444` ≈ 6.0:1
   - `.footer-area p` `#888888` on `#000000` ≈ 5.92:1
   - `.badge-row` `#999999` on `#000000` ≈ 6.66:1
   - `.intro-text` `#dddddd` on `#0a0a1a` ≈ 13.2:1
   - `.feature-box p` `#aaaaaa` on `#0a0a2a` ≈ 5.4:1
   - `.checklist li` `#cccccc` on `#0a0a2a` ≈ 14.5:1
   - `.error-box h3` `#ff4444` on `#1a0a0a` ≈ 5.5:1
   - `.no-photo` `#9999bb` on `#0a0a1a` ≈ 6.5:1
   - All clear the 4.5:1 normal-text minimum. **No finding.**

2. **Touch-target size (WCAG 2.5.8 AA, 24×24 CSS px minimum)** — primary nav (~32px ✓), `.retro-button` (~40px ✓), `.retro-button-small` (`min-height: 32px` ✓ `style.css:311`), gallery thumb tap zone (90×90 ✓ `style.css:389-394`), generated-page `.nav-bar a` (~28–34px ✓ post-N15 `createAct.py:525-529`). Inline links exempt under WCAG 2.5.8 inline exception. **No finding.**

3. **Semantic structure & landmarks (WCAG 1.3.1, 2.4.1)** — framework pages: single `<h1>` (`base.html:19`), `<header role="banner">` (`base.html:17`), `<nav aria-label="Primary">` (`base.html:27`), `<main id="main-content">` (`base.html:34`), `<footer role="contentinfo">` (`base.html:39`) all present. Skip link present (`base.html:12`). Generated band page: `<header role="banner">` (`createAct.py:548`), `<nav aria-label="Page sections">` (`createAct.py:558`), `<h1 class="band-title">` (`createAct.py:550`). N16 (no `<main>`/`<footer>` on generated page) remains MEDIUM deferred. **No new finding.**

4. **Heading hierarchy** — N14 (`<h2>` → `<h4>` skip in feature boxes at `index.html:85,91,97`) carries over LOW. Other pages have correct `h1 → h2 → h3` hierarchy. Generated page: `h1` (band-title) → `h2` (section-headers) clean.

5. **ARIA & live regions** — `aria-live="polite"` on `#progressDisplay` (`generate.html:33`), `#successDisplay` (`generate.html:88`); `role="alert" aria-live="assertive"` on `#errorDisplay` (`generate.html:109`). `aria-current="page"` on active nav link (`base.html:28-30`). `aria-hidden="true"` on decorative star strips (`base.html:18,23`; `createAct.py:549,554`). `aria-label="Page sections"` on generated-page nav (`createAct.py:558`). Redundant `role="banner"` on `<header>` and `role="contentinfo"` on `<footer>` (both implicit by HTML5 spec) are lint-level noise, not violations — left as-is. **No finding.**

6. **Keyboard / focus** — `:focus-visible` outline (yellow `#ffff00`, 3px) on all interactive elements (`style.css:40-48`); skip-link visible on focus (`style.css:19-24`); generated-page anchors have `:focus-visible` outline (`createAct.py:394-397`). `focusHeading()` (`generate.html:211-217`) moves focus to the new region's heading on state transitions; the three headings carry `tabindex="-1"` (`generate.html:35, 90, 111`). Suppression of default outline on `main:focus` and `h3[tabindex]:focus` (`style.css:53-56`) is restricted to non-keyboard focus — keyboard navigation still gets the `:focus-visible` outline. **No finding.**

7. **Reduced motion (WCAG 2.3.3 AAA, plus mechanism for 2.2.2 A)** — site stylesheet (`style.css:457-472`), generated-page inline `<style>` (`createAct.py:497-504`), and `static/js/main.js:6-10` all honour `prefers-reduced-motion: reduce`. Generated page also gates the `bandTitlePulse` text-shadow animation behind `@media (prefers-reduced-motion: no-preference)` (`createAct.py:427-435`). Footer `.blink` flash frequency ~0.83 Hz (`style.css:450`, `createAct.py:492`), well below WCAG 2.3.1's 3-flash threshold. **No finding.**

8. **Image alt text** — gallery thumbs (`gallery.html:25` "Promotional photo of {band.name}"), generated band photo (`createAct.py:577` "Promotional photo of {band_name}") all descriptive; decorative stars `aria-hidden="true"`. **No finding.**

9. **Lang / `<title>` / viewport** — present on all four framework templates (`base.html:2,5,6`) and on the generated band page (`createAct.py:377,379-381`). **No finding.**

10. **Empty state** — gallery has clear empty-state with CTA (`gallery.html:43-52`). **No finding.**

11. **Form/input accessibility** — N/A: app has no input forms; only buttons. All five `<button>` elements carry explicit `type="button"` (`generate.html:26, 80, 97, 101, 113`). **No finding.**

12. **Responsive layout** — `@media (max-width: 640px)` (`style.css:481-506`) collapses tables to 100%, drops thumb to 60×60, reduces title size, stacks feature cells. Re-confirmed no horizontal overflow at 320px / 768px / 1024px on the framework pages. Generated page: `.photo-frame img { max-width: 500px; width: 100%; }` (`createAct.py:447-453`) — image scales down properly; `.backstory-box { width: 90%; }` (`createAct.py:460-469`) is fluid; tables use `width="90%"` HTML attribute. **No finding.**

13. **UX anti-patterns** — placeholder `href="#"` links in generated band page (`createAct.py:599, 602-604, 620`) are deliberate retro pastiche, documented as deferred. `cancelGeneration()` truth-in-labelling (N8) remains MEDIUM deferred — clicking [ CANCEL ] only stops the client-side poll, server keeps running. Not a regression; long-standing carry-over.

14. **Path-traversal & input validation** — `SAFE_BAND_NAME = re.compile(r'^[A-Za-z0-9_\-]+$')` (`app.py:21`) applied at both `view_band` (`app.py:73`) and `band_assets` (`app.py:85`). **No finding.**

15. **JavaScript graceful degradation** — N17 (no `<noscript>` on `/generate`) carries LOW.

16. **Drift between `templates/` and `createAct.py`-generated HTML** — re-walked specifically for N18-class issues (this is the category where N18 hid for nine days):
    - `members-table` (`createAct.py:343-352, 470-475, 586-588`) uses `<tr><td><td></tr>` for name+instrument with no column-header row. Each row is a self-contained unit — name in cell 1, instrument in cell 2, bio in colspan=2 below. Closer to a layout/list pattern. Name cell could arguably use `<th scope="row">` for stronger row-header semantics, but the structure has no column headers, so no `<th scope="col">` requirement under SC 1.3.1. **Not a finding this pass.** Watch-item: if a future refactor adds a "Name | Instrument" header row, `<th scope="col">` would become required (and the equivalent missing-th pattern from N18 would re-emerge).
    - Discography per-album tables (`createAct.py:361-371`) use a single colspan=2 cell for the album title and a single content cell for the track list — no column-header pattern, no `<th>` needed. The track listing inside is a semantic `<ol>` (`createAct.py:367-369`), correct for ordered tracks.
    - All three framework-template tables (`info-table` `index.html:28-33`, `gallery-table` `gallery.html:14-19`, `progress-steps-table` `generate.html:44-48`) use `<th scope="col">` correctly post-N18 fix.
    - Generated-page `<header>`/`<nav>` landmarks present; `<main>`/`<footer>` absent (N16 MEDIUM, deferred).
    - Generated-page skip link absent — but the page has only one major content region with five same-page anchor links in the nav (Backstory, Band Photo, Members, Discography, Guestbook). The five-item nav itself functions as a skip mechanism. Not a 2.4.1 failure under SC interpretation. **No finding.**
    - `index.html` "What You Get" table (`index.html:81-102`) uses `<table>` for layout with no `<th>` cells — this is L12 (table-for-layout), already a LOW carry-over. The cells contain semantic `<h4>` + `<p>` content (the N14 h2→h4 skip is logged separately). No new finding.

## Carried-over MEDIUM / LOW (still deferred — unchanged)

| ID | Severity | Item | File:line |
|---|---|---|---|
| N16 | MED | Generated band page lacks `<main>` and `<footer>` elements | `createAct.py:543-624` |
| M7 | MED | Dead `marquee {}` rule in generated CSS | `createAct.py:530-536` |
| M8 | MED | `bandPreview.innerHTML` injects unsanitised AI band name | `templates/generate.html:233-236` |
| N5 | MED | Progress-steps table column header is the literal "?" | `templates/generate.html:46` |
| N6 | MED | Step-indicator state changes not announced to SR users | `templates/generate.html:183-209` |
| N7 | MED | Polling interval re-announces same prose message | `templates/generate.html:158, 183-191` |
| N8 | MED | `cancelGeneration()` does not tell the server to stop | `templates/generate.html:249-254`, `app.py` |
| N9 | LOW | `#progressDisplay` `aria-live` region wraps too much markup | `templates/generate.html:33-85` |
| N10 | LOW | Failing step is invisible to user on error | `templates/generate.html:49-77` |
| N12 | LOW | `body` font-family includes `cursive` between specific and generic | `style.css:67`, `createAct.py:387` |
| N13 | LOW | `<hr>` `noshade` and `size` attributes obsolete in HTML5 | multiple templates and `createAct.py` |
| N14 | LOW | Heading hierarchy h2 → h4 skip in feature boxes | `templates/index.html:85,91,97` |
| N17 | LOW | No `<noscript>` fallback on `/generate` | `templates/generate.html:14-30` |
| L7 | LOW | `Faker==28.1.0` declared but unused | `requirements.txt` |
| L8 | LOW | Inline `import re` inside functions | `createAct.py:85, 218` |
| L9 | LOW | Visitor counter randomises on each page load (thematic) | `templates/base.html:47` |
| L10 | LOW | Dead `mailto:webmaster@aibandgen.geocities.com` (thematic) | `templates/base.html:52` |
| L11 | LOW | Visitor counter and `mailto` repeated in generated band pages (thematic) | `createAct.py:600, 616` |
| L12 | LOW | `<table>` used for layout in `index.html` "What You Get" section | `templates/index.html:81-102` |
| L13 | LOW | Inline styles throughout templates and generated HTML | many |
| L14 | LOW | Deprecated HTML attributes (`bordercolor`, `noshade`, `cellpadding`, `align`, `width`) throughout | many |
| L15 | LOW | `band_assets()` route doesn't whitelist filenames | `app.py:82-87` |

The broader f-string-interpolated XSS surface in `create_html_content()` (sibling to M8) remains logged-for-tracking, MEDIUM-severity, deferred.

## Summary

| Severity | Count this audit | Fixed this pass |
|---|---|---|
| CRITICAL | 0 | — |
| HIGH | 0 | — |
| MEDIUM | 0 new (7 carried) | 0 (deferred) |
| LOW | 0 new (14 carried) | 0 (deferred) |

**No critical/high findings. No code changes this pass.** Audit log committed to `fix/code-review-2026-05-11` per the established cadence; PR opened against `main`.

## Phase 3 — Verification

- `python3 -c "import ast; ast.parse(open('app.py').read()); ast.parse(open('createAct.py').read())"` — both files compile cleanly.
- Jinja2 template parse: `base.html`, `index.html`, `gallery.html`, `generate.html` all loaded cleanly via `Environment(loader=FileSystemLoader('templates'))`.
- `git diff cc178bf..HEAD -- '*.py' '*.html' '*.css' '*.js'` — empty (no code drift since yesterday's audit-only commit).
- Pre-existing generated band pages on disk re-inspected: not regenerated this pass (would consume API credits and overwrite dated artifacts); their accessibility quirks are pre-existing and not caused by current code.
- Did not start the dev server in-process (would burn API credits on `/api/generate`). Static template + source review covers all rendering paths the audit needs.

## Phase 4 — Deliver

- Branch `fix/code-review-2026-05-11` created off `fix/code-review-2026-05-10` (prior daily fix branches stacked and not yet merged into `main`).
- Commit includes only `tasks/audit-2026-05-11.md` and `tasks/review.md` (audit log update). Untracked `media/` directory not staged.
- PR raised via `gh pr create` targeting `main`. URL recorded in agent's final response.
